# 进销存管理系统运行说明

## 运行环境

- Windows 10/11
- Qt 6.8.3 MinGW 64 位（Quick、QML、Widgets、SQL、Network、Charts 模块）
- MinGW 13.1 64 位
- MySQL 8.x，且 Qt 安装包含 `QMYSQL` 驱动

## 配置运行参数

应用从可执行文件同级的 `config/app.ini` 读取运行参数。仓库仅提交示例文件 [config/app.ini.example](config/app.ini.example)，其中没有真实凭据。

首次配置时，创建可执行文件目录下的 `config` 文件夹，并复制示例文件：

```powershell
New-Item -ItemType Directory -Force .\config
Copy-Item .\config\app.ini.example .\config\app.ini
```

随后编辑 `config/app.ini`：

- `[userDatabase]`：登录、注册和 AI 会话使用的 MySQL 连接；
- `[businessDatabase]`：库存、交易、收支与可视化使用的 MySQL 连接；
- `[ai]`：DeepSeek API 密钥、接口地址、模型、流式开关和系统提示词；
- `[application]`：Qt Quick Controls 样式、应用图标和 QML 启动入口。

`config/app.ini` 已被 Git 忽略，勿提交真实数据库口令或 AI 密钥。也可通过 `QML_PRODUCT_MANAGER_CONFIG` 环境变量指定任意位置的 INI 文件，例如：

```powershell
$env:QML_PRODUCT_MANAGER_CONFIG = "D:\配置\qmlProductManager.ini"
```

## 初始化数据库

以具备建库权限的 MySQL 用户执行 [database/schema.sql](database/schema.sql)：

```bash
mysql -u root -p < database/schema.sql
```

脚本会创建：

- `user_management`：用户、AI 会话及消息表；
- `ecjtu_market`：分类、库存、总交易、支出与收入表。

首次注册的账号默认是顾客访客（权限值 `3`）。使用下列 SQL 调整演示账号角色：

```sql
UPDATE user_management.users SET permission = 1 WHERE username = '店主账号';
UPDATE user_management.users SET permission = 2 WHERE username = '店员账号';
```

配置文件的 `[userDatabase]` 和 `[businessDatabase]` 分别对应两个数据库。执行脚本前请确保 `app.ini` 中的连接参数与实际 MySQL 服务一致。

## 配置 AI 小助手

AI 小助手使用 DeepSeek 的 OpenAI 兼容 Chat Completions 接口。请在 `config/app.ini` 的 `[ai]` 节设置 `apiKey`；`baseUrl` 和 `model` 可保持默认值，`stream` 当前应设为 `false`。密钥不能写入 QML、C++ 源码、资源文件或 Git。

配置示例：

```ini
[ai]
apiKey=你的密钥
baseUrl=https://api.deepseek.com/chat/completions
model=deepseek-chat
stream=false
```

未配置密钥时，AI 页面会提示配置错误，不会发送网络请求。

## 构建

在 Git Bash 中执行。修改 [qmlProductManager.pro](qmlProductManager.pro) 或 [qml.qrc](qml.qrc) 后，必须先重新运行 qmake。

```bash
export PATH="/e/Qt/Tools/mingw1310_64/bin:$PATH"
QMAKE="/e/Qt/6.8.3/mingw_64/bin/qmake.exe"
MINGW_MAKE="/e/Qt/Tools/mingw1310_64/bin/mingw32-make.exe"
BUILD_DIR="build/Desktop_Qt_6_8_3_MinGW_64_bit-Debug"

mkdir -p "$BUILD_DIR"
"$QMAKE" -o "$BUILD_DIR/Makefile" qmlProductManager.pro -spec win32-g++ "CONFIG+=debug" "CONFIG+=qml_debug"
"$MINGW_MAKE" -f "$BUILD_DIR/Makefile" -j1
```

运行 Debug 程序：

```bash
./debug/qmlProductManager.exe
```

如果 qmake 在非 ASCII 路径下提示“系统找不到指定的路径”，可在项目根目录直接运行 qmake，再使用根目录生成的 `Makefile` 构建。若编译器提示 `cc1plus.exe: out of memory`，请关闭占用内存的程序、增大 Windows 分页文件后再次串行构建。

## 手动验收清单

### 权限

- 店主：可访问全部页面并完成分类、入/出库、售价和库存阈值修改。
- 店员：可入/出库、查看收支记录和可视化；添加分类、修改售价和阈值均应被拒绝。
- 顾客访客：导航只显示库存和 AI；库存隐藏进价与上下限，且不显示预警、过期、收支状态色；资料和退出入口仍可用。

### 库存与收支

- 验证分类重复、空字段、零/负数量、非法日期、保质期早于生产日期、上限低于下限、库存不足出库均被阻止。
- 完成一笔入库和一笔出库后，确认库存、总交易、支出、收入同步更新。
- 按分类、商品和日期范围筛选三类收支记录，确认汇总金额与表格数据一致；总交易的支出为浅红、收入为浅绿。

### 可视化与 AI

- 成本、销售额、利润均验证分类模式的饼图/柱图/折线图，以及商品模式仅显示折线图。
- 利润定义为同一筛选范围内的销售收入减采购支出。
- 验证 AI 的未配置密钥、网络失败、正常回复、新建会话及切换会话；确认不同账号之间不能读取彼此历史记录。
