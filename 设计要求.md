### 角色权限管理
1、店主：拥有最大权限，可查看所有信息进行所有操作。
2、店员：不可添加分类、更改售价、更改警告值，其余操作和页面均可查看操作。
3、顾客访客：只有库存情况子页面和AI小助手界面，可查看库存情况但看不到上下限阈值及进价信息，表格无特殊颜色显示，与AI助手进行对话，其余操作和页面均不可查看操作。

### QML 页面组织

`qml/MainMenu.qml` 是业务壳层：左侧导航控制右侧子页面，其索引依次对应库存情况，收支记录（总交易记录、支出记录、收入记录），数据可视化（成本统计、销售额统计、利润统计）和 AI小助手界面。

子页面按领域分组：
- `qml/StockManagement/`（库存情况子页面）：
顶部工具栏第一项“管理”可展开，依次是”添加分类“（添加商品分类名到数据库，分类名不能重复且为必填项），”入库“（选择分类（库存已有分类），填写商品名称、数量（正整数）、进价（正数）、保质期（有特定格式）后添加商品到库存，所有信息均为必填项），”出库“（选择分类、分类下对应的商品名称，填写数量（正整数，不得超过库存数量），选择后显示售价不可修改展示带“￥”，所有信息为必填项）；第一项“设置”可展开，依次是“更改售价”（选择分类、分类下对应的商品名称，选择后显示进价带”￥“不可修改，售价默认显示数据库中数据可修改（正数））、”更改警告阈值“（设置商品数量上下限阈值，选择分类、分类下对应的商品名称，选择后显示之前设置的上下限阈值（非负整数）、现有数量（不可修改））。
下面是库存情况表，字段包含商品分类、商品名称、数量、进价、售价、上下限阈值、生产日期、保质日期，数量不在上下限阈值范围内时数量单元格背景为浅黄色，商品过期时生产日期、保质日期单元格背景为浅红色。
库存情况表上方有一行条件筛选，用户可根据排序方式（按进价排序、按售价排序、按数量排序），复选框选择升序或降序，商品分类、商品名称等字段进行筛选。
- `qml/CheckRecords/`（收支记录子页面，包含总交易记录、支出记录、收入记录）：1、总交易记录表格字段包含交易编号、分类名、商品名、交易单价（正数）、数量（正整数）、交易金额（支出为负，收入为正）、交易时间（具体到年月日时分秒），属于支出则该行背景为浅红色，属于收入则该行背景为浅绿色。
表格上方有一行条件筛选，用户可根据分类名、商品名、交易时间范围（年月日）字段进行筛选，统计所有交易总额并显示。
2、支出记录表格字段包含交易编号、分类名、商品名、进价（正数）、数量（正整数）、支出（正数）、交易时间（具体到年月日时分秒）。
表格上方有一行条件筛选，用户可根据分类名、商品名、交易时间范围（年月日）字段进行筛选，还统计总支出金额并显示。
3、收入记录表格字段包含交易编号、分类名、商品名、售价（正数）、数量（正整数）、收入（正数）、交易时间（具体到年月日时分秒）。
表格上方有一行条件筛选，用户可根据分类名、商品名、交易时间范围（年月日）字段进行筛选，还统计总收入金额并显示。
- `qml/DataVisualization/`（数据可视化子页面，包含成本统计、销售额统计、利润统计）：3个子页面展示布局大致相同，每个子页面包含一个图表组件（饼图、柱状图、折线图），用户可根据需要筛选数据（分类名、商品名称、统计日期尺度、起始终止日期范围），图表会实时更新，在只选择了分类名没选择具体商品的情况下，饼图、柱状图会显示该分类下的所有商品的统计数据（销售额/成本/利润），折线图横轴是时间纵轴是该分类所有商品总统计数据（销售额/成本/利润），在同时选择了分类名和具体商品名的情况下，饼图、柱状图不显示，只显示折线图，横轴是时间纵轴是该商品统计数据（销售额/成本/利润）。
- `qml/AIAssistant/` （AI小助手界面）：调用Deepseek v4 flash模型，密钥是sk-6906c13853f64e99a2f182cbae09b883，用户可输入有关库存管理相关问题点击“发送”按钮触发AI模型的调用，AI模型会返回回答实时在界面上显示，在右侧用户可新建或查看对话记录。
- `qml/Person/`（个人信息页面）：展示个人信息，可退出登录。
- `qml/Login&Register/`：登录、注册和数据库连接错误页。登录页使用 `Qt.labs.settings` 的 `user_config.ini` 保存“记住密码”状态。

